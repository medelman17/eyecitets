/**
 * Data layer: Reporter database and external data integration
 *
 * This module is exported as a separate entry point (./data) to enable
 * lazy loading of reporter data without bloating the core bundle.
 */

export * from "./reporters"
export * from "./knownCodes"
